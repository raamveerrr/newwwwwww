# Copy this file to .env and fill in your actual values
# Remove this file from version control

# Razorpay Configuration (Required)
VITE_RAZORPAY_KEY_ID=your_razorpay_test_key_here

# Firebase Configuration (Auto-configured)
# These are already set in firebase.js, no need to change

# Note: 
# - All environment variables MUST start with VITE_ to be accessible in the frontend
# - Never expose your Razorpay Key Secret in frontend code
# - For production, update Razorpay key to live key